import 'package:flutter/material.dart';

class OthererPage1 extends StatelessWidget {
  const OthererPage1({Key? key}) : super(key: key);
  static const route = "/otherer-page-1";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
